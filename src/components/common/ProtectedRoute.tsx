// components/common/ProtectedRoute.tsx
// Backward-compatible alias — use RequirePermission for new code.

export { RequirePermission as ProtectedRoute, RequirePermission as default } from "../../auth/guards/RequirePermission";
